logging:
  level:
    root: INFO

# ssl-konfiguraatio
server:
  port: 8555
  ssl:
    key-store-type: PKCS12
    key-store: classpath:localhost-keystore.p12
    key-store-password: oma-opiskelijavalintapass
    key-alias: oma-opiskelijavalinta
    enabled: true
  servlet:
    context-path: /oma-opiskelijavalinta

oauth2:
  client: unset
  secret: unset

spring:
  profiles:
    active: dev
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev.otuva.opintopolku.fi/kayttooikeus-service
  datasource:
    url: jdbc:postgresql://localhost:55455/test
    username: app
    password: app
  flyway:
    baseline-on-migrate: true

app:
  db:
    threads: 2

